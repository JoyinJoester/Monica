<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:vmSafeMode="true">
        <!--
          Work around emulator/runtime crashes in ART profile saving that currently
          surface around generated Compose / Kotlin serialization classes.
          Keep release behavior intact and hard-disable debug-side profile/JIT
          optimizations that route through the crashing Profile Saver path.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            tools:node="merge">
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                tools:node="remove" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            tools:node="remove" />
    </application>
</manifest>
